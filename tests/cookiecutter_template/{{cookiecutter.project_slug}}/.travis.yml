language: python
python:
  - "3.11"

install:
  - pip install -U pip
  - pip install -e .

script:
  - pytest -q
{% if cookiecutter.use_pypi_deployment_with_travis == "y" %}
deploy:
  provider: pypi
  user: "{{ cookiecutter.github_username }}"
  password:
    secure: "insecure-token"
{% endif %}
