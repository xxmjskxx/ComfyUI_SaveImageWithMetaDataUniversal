name: SaveImageWithMetaDataUniversal CI

on:
  push:
    paths:
      - 'custom_nodes/ComfyUI_SaveImageWithMetaDataUniversal/**'
  pull_request:
    paths:
      - 'custom_nodes/ComfyUI_SaveImageWithMetaDataUniversal/**'

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        working-directory: custom_nodes/ComfyUI_SaveImageWithMetaDataUniversal
        run: |
          python -m pip install --upgrade pip
          pip install -U pytest pillow numpy piexif ruff

      - name: Lint (ruff)
        working-directory: custom_nodes/ComfyUI_SaveImageWithMetaDataUniversal
        run: |
          ruff check --exit-zero .

      - name: Run tests
        working-directory: custom_nodes/ComfyUI_SaveImageWithMetaDataUniversal
        env:
          METADATA_TEST_MODE: '1'
        run: |
          pytest -q

  lint-strict:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install ruff
        run: |
          python -m pip install --upgrade pip
          pip install ruff
      - name: Ruff strict (fail on issues)
        working-directory: custom_nodes/ComfyUI_SaveImageWithMetaDataUniversal
        run: |
          ruff check .
